<%= form_for [@product, @comment] do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :rating, value: 0.0 %>

  <%= f.text_area :content, class: "form-control" %>
  <br>
  <%= f.submit yield: :button_text , class: "btn btn-primary" %>
<% end %>
