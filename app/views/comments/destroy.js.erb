$('#delete-<%=@comment.id%>-comment').html('');
$('#rate-score').find('.average-comment-rating').raty({
    readOnly:true,
    path: '/assets/',
    score: function() {
      return $(this).attr('data-score');
    }
  });
$('#rate-score').html('<div class="average-comment-rating" data-score="<%= @product.rate_score %>"></div>');
