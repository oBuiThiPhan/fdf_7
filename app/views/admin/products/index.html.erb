<h1><%= t "views.admin.product.index.all" %></h1>

<table class="table table-hover">
  <tr>
    <th><%= t "views.admin.product.index.id" %></th>
    <th><%= t "views.admin.product.index.name" %></th>
    <th><%= t "views.admin.product.index.category" %></th>
    <th><%= t "views.admin.product.index.price" %></th>
    <th><%= t "views.admin.product.index.quantity" %></th>
    <th><%= t "views.admin.category.index.function" %></th>
  </tr>
  <%= paginate @products%>
  <% @products.each do |product| %>
    <tr>
      <td><%= product.id %></td>
      <td><%= product.name %></td>
      <td><%= product.category.title %></td>
      <td><%= product.price %></td>
      <td><%= product.quantity %></td>

      <td>
        <span>
          <%= link_to t("views.admin.category.index.editbutton"),
            edit_admin_product_url(product), class:"btn btn-info" %>
          <%= link_to t("views.admin.category.index.deletebutton"),
            admin_product_url(product), method: :delete,
            data: {confirm: t("views.admin.category.index.deleteconfirm")},
            class:"btn btn-danger" %>
        </span>
      </td>
    </tr>
  <% end %>
</table>

<%= link_to t("views.admin.product.index.newproductbutton"),
  new_admin_product_path, class: "btn btn-info" %>
